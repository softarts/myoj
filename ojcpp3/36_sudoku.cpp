#include <vector>
using namespace std;

/*
很有意思的一道题目，她并不需要sudoku是可以解的
那实质只要判断已有的数字在row, col,block中有没出现过
*/
class Solution {
public:
    bool isValidSudoku(vector<vector<char>>& board) {
        
    }
};